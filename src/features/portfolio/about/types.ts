export interface CurrentlyLearning {
  title: string
  items: string[]
}

export interface AboutData {
  title: string
  story: string[]
  currentlyLearning: CurrentlyLearning
  interests: string[]
}
