import { Brain, Cloud, Code2, Layout, Wrench, type LucideIcon } from 'lucide-react'
import type { SkillGroupIcon } from '../types'

export const SKILL_GROUP_ICONS: Record<SkillGroupIcon, LucideIcon> = {
  Code2,
  Brain,
  Layout,
  Wrench,
  Cloud,
}
