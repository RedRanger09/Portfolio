/** Small, deliberately quiet footer — the dashboard equivalent of the public `Footer`, without duplicating its content. */
export function AdminFooter() {
  return (
    <footer className="border-t border-white/[0.08] px-4 py-6 sm:px-6">
      <p className="text-xs text-zinc-600">Akshay Tiwari — Admin</p>
    </footer>
  )
}
